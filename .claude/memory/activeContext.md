# activeContext.md — Y-17 100 gipotez

## Scope Fence
- **[DERIVED + VERIFIED-COMPUTATION — 2026-09-15, H-CAT31-3 Point 64 energy anatomy BOTH-STABLE-PILOT; status unchanged]:** Tao prime uncertainty plus `2Q` forced time-domain zeros gives `s=|supp y|>=2Q+1`, while positive mass one gives exact `n||y||2^2=(n/s)(1+CV_support^2)`. Conditional binomial Q makes `E[n/s]` absolutely bounded, so the live POL burden is weight unevenness and its rare-density dependence, not frequency sparsity. On frozen 12+12+12 graphs, mean n/s=`1.890/2.025/2.001`, mean 1+CV2=`1.980/1.970/2.040`, endpoint ratios `1.059/1.030`; primary support equals `2Q+1` in all 36, threshold spread max 3.90%, independent exact arithmetic/binomial audit passes. Verdict `BOTH-STABLE-PILOT`, NOT moment control. Next lemma must genuinely bound moments of CV for the positive optimal basic measure; renaming it conditioning/leverage is not progress. Artifact: `.../ENERGY_ANATOMY_RESULT.md`.
- **[DERIVED + VERIFIED-COMPUTATION — 2026-09-15, H-CAT31-3 Point 63 cross-complement energy route SURVIVES-PILOT; status unchanged]:** for old `G0`, new `G1=G0 union H_i`, and positive-Fourier optima `x` of `G0`, `w` of `complement(G1)`, support intersection plus Parseval and `theta(G1)theta(complement G1)=n` give `delta_i<=2x_iw_i`. The unique minimum-L2 selector is equivariant; conditional complement symmetry, Cauchy, and prime orbit transitivity yield `|lambda_n|<=2(E||x*||2^2-1)=2(nE||y*||2^2-1)`. Frozen 12+12+12 graph pilot at n=127/509/2039: mean energy `3.745/3.999/4.083`, endpoint mean/median/q90 ratios `1.090/1.131/0.961`, mean-ratio CI `[0.989,1.197]`; no factor-2 kill. Independent KKT certificates (zero stationarity residual), six direct large cross-complement checks, and exhaustive conditioned n=11 pass. Verdict `SURVIVES-PILOT`, NOT bounded energy. The published random-nullspace proof yields a derived `O(log^3 n)` energy/susceptibility ceiling; the sole new target `(POL)` must use positivity+optimality to improve this to O(1). Multiplier route is secondary; composite n and `R_n` remain OPEN. Artifacts: `.../CROSS_TURAN_ENERGY_THEORY.md`, `.../OPTIMAL_ENERGY_RESULT.md`.
- **[VERIFIED-COMPUTATION/DERIVATION — 2026-09-15, H-CAT31-3 aggregate shadow-budget arc corrected and parked; status unchanged]:** Point 60's random all-coordinate test (16 graphs each n=31/61/127, 850 independently rechecked coordinate LPs) had mean normalized budget 1.286/2.241/2.256 and max 2.813/3.564/3.966, so its finite-sample gate said `SURVIVES-WITH-C4`. Point 61 then checked the missed extreme: for the empty graph `B=m[n-theta(C_n)]/n~n/4`, numerically 31.505 at n=127, killing every universal constant budget. Point 62 tested the post-hoc density correction `B<=2m/(Q+1)` on exhaustive n=11/13/17 and 32+32 fresh n=127/251 graphs; three fresh counterexamples, worst ratio 1.1481, independently reproduced to 3.16e-13. Verdicts: `KILLED-DETERMINISTIC-BUDGET`, `KILLED-DB2`; do not tune the constant. A general analytically-derived `C m/(Q+1)` could still imply bounded ensemble expectation, but this variant family is parked pending new geometry. Live result remains Point 58's factor-3 rectangle; live gap is distributional E[AK], plus R_n and composite n. Artifacts: `.../SHADOW_BUDGET_RESULT.md`, `.../DENSITY_BUDGET_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-15, H-CAT31-3 fresh shadow-tail audit INCONCLUSIVE; status unchanged]:** Point 59 used 48+48 fresh independent endpoint graphs, one tested orbit each. Endpoint L2/L4 ratios are A=`0.949/0.861`, K=`1.157/1.142`, AK=`1.351/1.437`; no bootstrap CI lies wholly above 1, so the moment target is not killed, but AK upper limits `2.109/2.060` miss the stronger `<2` survival gate. Joint tail is decisive: top 10% supply 95.0% of AK's fourth empirical moment at n=2039; Pearson/Spearman A-K dependence `0.896/0.922`. DS/IPM agree 96/96; independent summaries/bootstrap plus four direct LP reconstructions pass. Verdict `INCONCLUSIVE`, not uniform-moment evidence. Do not repeat identical MC; next candidate is an aggregate all-absent-coordinate shadow budget whose exchangeable average would directly control E[AK]. Artifact: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/SHADOW_TAIL_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-15, H-CAT31-3 factor-3 shadow moment pilot survives; status unchanged]:** Point 58 removed two unnecessary Point 57 burdens. For odd `n`, the added orbit graph has chromatic number 3, so standard theta monotonicity/additivity gives `theta_new>=theta_old/3`; weak Lagrangian duality then gives the deterministic rectangle `delta_i <= (3 log 3/(2n)) A_i K_i`, `A=|alpha|`, `K=n|x_i^circ|/theta`, for any old optimizer. Thus uniform conjugate moments, especially `L2-L2`, suffice without an optimal-face maximum or a separate distance-from-singularity condition. The old `A<=16` grid bracket was censored and is not tail evidence; Point 58 used actual equality-dual multipliers. On fixed 12+12+12 graphs, all six endpoint L2/L4 ratios for A/K/AK are `0.909..0.970`, DS/IPM multipliers coincide in 36/36, mean rectangle tightness is `0.534/0.508/0.541`, and independent arithmetic/direct-LP/exhaustive-n11 verification passes. Verdict `SURVIVES-PILOT`, NOT uniform-moment evidence: AK bootstrap upper ratio limits are 2.16/2.31 because 12 graphs are tail-underpowered. Next action is a larger independent-graph tail audit; the one proof target afterward is uniform moments. Main variance theorem, `R_n`, and composite `n` remain OPEN. Artifact: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/SHADOW_MOMENT_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-14, H-CAT31-3 signed shadow-price mechanism VIABLE; status unchanged]:** Point 57 concretely measured the missing dual-multiplier condition already named in Point 6; it is not a fifth independent LP theory. Corrected proposal before result: `alpha` must be signed, grid crossing is an upper bracket, and the joint area proxy is the estimand. Exact 16-case n=11 dual identity passed (max error `1.78e-15`). On fixed 12+12+12 graphs n=127/509/2039, mean A=`6.250/6.042/6.083`, H=`2.122/2.015/1.924`, nE[B_SP]=`19.518/15.803/15.749`; endpoint ratios `0.973/0.907/0.807`, all bootstrap growth CIs contain zero, all cases finite/bracketed, endpoint looseness `3.06x`. Independent arithmetic, different-seed 20k bootstrap, and direct LP spot checks pass. Large-n q0 uses a conservative `1e-7*theta`-optimal superlevel because literal exact-face equalities were numerically brittle. Verdict `VIABLE-MECHANISM`, not a theorem; H-CAT31-3 remains OPEN. Single next proof target: joint Lp/Lq/tail control of true signed width `a_i*` and `n*u_i/theta`, plus `a_i*u_i/theta<=rho<1`. Artifact: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/SHADOW_PRICE_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-14, H-CAT31-3 optimal-face slack mechanism killed; status unchanged]:** secondary LP maximized the true minimum post-deletion Fourier slack over the exact OLD optimal face, all frequencies, without adding `x_i=0` or optimizing perturbed value. On fixed 12+12 graphs n=127,509, face bound equals canonical to numerical precision: improvement `1.000000000/1.000000001`; n=509 looseness 15.59, nE[B] ratio 2.261, growth CI `[16.63,73.17]`. Pre-registered verdict `KILLED-AS-CURRENT-MECHANISM`; exhaustive n=11 and independent full-n formulation pass. Secondary point is itself within max L2 `3.9e-12/1.75e-8` of canonical. Do NOT overclaim singleton face: result only says no old-face point improves this maximin slack functional. Explicit primal repairs now parked; next priority direct dual/value curvature. Artifact: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/OPTIMAL_FACE_RESULT.md`.
- **[VERIFIED-ANALYSIS — 2026-09-14, H-CAT31-3 proposed Stein/M_-1 step rejected before compute; status unchanged]:** внешний текст предложил как новый 1–2h test уже выполненный Point 42 расчёт. Zero-cost audit: `0.51->0.39` есть ratio `M_-1/(C_q/gamma_1)`, не сам M; actual `M_-1=0.03192->0.02345`, descriptive 7-point slope `-0.422`. Более существенно, implication неверна: даже `M_-1=O(1/n)` даёт максимум central-layer `C_q=O(1/n)`, тогда как `Var(X)<=m E[delta_i^2]/4`, `m=Theta(n)`, требует layer-aggregated `E[delta_i^2]=O(1/n^2)`, включая `A_q^2` и все слои. Identity `Var(h)=<h,L L^-1h>` tautological; direct second-order Gaussian Poincare controls normal approximation after variance normalization, not unknown variance scale. Verdict `DUPLICATE-AND-INVALID-IMPLICATION`; no new theta/CG solve. Также не принимать overclaim, что n^-1/2 barrier доказан для *любого* sparse-vertex repair: проверены только две explicit constructions. Artifact: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/STEIN_MNEG1_AUDIT_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-14, H-CAT31-3 local-slack repair killed; status unchanged]:** выполнен следующий заранее зафиксированный Lovasz-specific цикл. Из старого optimum `x,y=Fx/n` построен локальный repair: минимальное uniform mixing `eta_i` восстанавливает positivity после удаления только пары `x_i,x_-i`; exact implication `delta_i<=B_i^loc`, `E B_i^loc=O(1/n) => |lambda_n|=O(1)`. Exhaustive n=11 и независимая full-dimensional LP проверка прошли. Но pre-registered gate убил сертификат: mean B `0.2845/0.1605/0.08146` при n=127/509/2039; nE[B] endpoint ratio `4.597`, endpoint looseness `32.23`. Он улучшает global convolution в `6.89x`, но масштаб mean B ≈ n^-0.451 следует `mean|x_i|≈n^-0.502`, а не real delta≈1/n. Не повторять global convolution или uniform-interior local mixing; следующий admissible объект — redistribution внутри old optimal/near-optimal face либо direct dual curvature, без tautological perturbed re-solve. Артефакт: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/LOCAL_SLACK_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-14, H-CAT31-3 Fourier lemma kill; status unchanged]:** после выбора susceptibility сформулирована до вычислений одна Lovasz-specific sufficient lemma: positive-frequency optimizer `y` ремонтируется global convolution с probability measure `nu`, зануляющей новую частоту. Точное следствие `delta_i<=B_i=-log max (y*nu)_0/y_0`; `E B_i=O(1/n)` дало бы `|lambda_n|=O(1)`. Детерминированная оценка выдержала exhaustive n=11 (max violation `1.22e-15`), но pre-registered practical gate убил сертификат: mean `B=0.406/0.491/0.561` при n=127/509/2039, реальный mean delta `0.04198/0.01030/0.00253`; endpoint looseness `222x`, а `n E B` выросло `22.18x`. Вердикт только для canonical global-convolution repair: `KILLED-AS-CURRENT-CERTIFICATE`; основная гипотеза OPEN. Следующая допустимая лемма должна быть localized/slack-based и не оптимизировать по всем repairs (это было бы tautological re-solve). Артефакт: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/CONVOLUTION_REPAIR_RESULT.md`.
- **[VERIFIED-COMPUTATION — 2026-09-14, H-CAT31-3 decomposition; status unchanged]:** реализован предложенный пользователем first-chaos разбор `V=W1+R`, основной `R` оценён cross-fit residual с поправкой на uncertainty обучения, algebraic U-statistic оставлен как cross-check; 5000 bootstrap refits и отдельная повторная реализация (`verify_decomposition.py`) сошлись. На старых n=127..2039 `W1/V≈0.61..0.91`, `nR≈0.76..1.05`; оба старых endpoint-контраста имеют CI с нулём, поэтому формальный gate шага 1 — `INCONCLUSIVE`. На независимых 128+128 свежих графах `nW1(2039)-nW1(127)=1.536`, CI `[0.447,2.562]`, тогда как `nR` не разрешён. **Практический единственный приоритет: susceptibility `lambda_n`; асимптотический статус остаётся OPEN.** Бесплатный falsification channel `g_n=E[theta]-sqrt(n)` уменьшается на старых и свежих endpoint-данных и не убивает `O(1/n)`. Артефакты: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/codex-20260914-susceptibility/DECOMPOSITION_PLAN.md` и `decomposition_results.json`.
- **Goal:** [PLAN, не факт] Оценить и начать проверку cross-domain мостов между каталогом ~141 открытых научных задач и реальными проектами (H-7 chromatin, ChernoffPy, May 1972)
- **[2026-09-09, ADR-087] Scope расширен пользователем явно:** пятый источник гипотез — сам 100-item каталог напрямую, без требования тематической связи с исходными 4 якорями. Не отменяет исходные якоря, добавляет к ним.
- **[VERIFIED — 2026-09-10] Deep external novelty audit — читать вторым (после ADR-121, перед ADR-122).** По прямому запросу пользователя запущены 6 параллельных `Agent(analyst)` по кластерам (Boolean network dynamics/control, Boolean structural/canalization, Forsythe, transient growth/Kreiss, TDA lakes, Lovász theta), каждый проверял ~33 сильных claim'а на 10 возможных отношений к внешней литературе (special case/counterexample/known-by-theorem/etc). **Критическая находка: все 6 агентов не имели WebFetch/arXiv** (agent-каталог снова соврал про tools — повтор `[AVOID×1]` из июля, теперь в 6× масштабе), поэтому внешняя половина `BLOCKED-INFRASTRUCTURE`. Вместо этого — дедуктивный реанализ: 9 claims оказались тавтологиями (H-B7-21/24 — буквально Lean-теорема/forward-closure; H1-VR time-reversal invariance — 5 строк из определения VR), 2 новых факта найдены в уже собранных данных (`Var(log θ/√n)∝n^-0.96` у Lovász; `K_ref` точно однородна степени 0 у Kreiss). Полный отчёт: `reports/2026-09-10-deep-external-novelty-audit.md`. **Track 1 (7 decisive-проверок, все локальные, сделаны в ту же сессию):** пересчитан Lovász slope (n≥40: ≈0, не −0.035) + явное неравенство E[θ]≥√n; K_ref-инвариантность подтверждена алгебраически и её численный разрыв на экстремальных T найден и задокументирован; исправлена «1-2 orders of magnitude» → точные 1.6-4.2 в H-B7-29; найден почти вакуумный floor-check в H-B7-31 (ROBUST-условия — 2-состояния графы); найдено и Pearl-зарегистрировано точное рациональное P(k=2)=2·P(k=1) в H-B7-26 (119/864=2×119/1728); гипотеза «chirality_excess просто переоткрывает baseline» ОПРОВЕРГНУТА корреляционной проверкой (H-B3-2); branch automorphism (H-B7-27) независимо подтверждён `pyboolnet`'s trap-space solver — verification_strength повышен medium→strong. **Track 2 (внешняя литературная проверка через WebFetch/arXiv/Semantic Scholar напрямую в основной сессии, не через агента — ЗАВЕРШЁН, 4/6 проверок).** Решающие находки: (1) arXiv:2609.04659 (Forsythe) — авторы САМИ прямой цитатой называют «smallest dimension» открытым вопросом, H-CAT37-2 бил в реальную открытую задачу; (2) arXiv:2502.16227 (Bandeira et al., companion work, найден и открыт) — Theorem 1 даёт ТОЧНО `√n ≤ E[θ] ≤ C√(n log log n)`, той же техникой (θθ̄=n + Jensen/AM-GM), опубликовано в феврале 2025 — `E[θ]≥√n` `KNOWN-BY-GENERAL-THEOREM`, НЕ собственная находка; но `Var(log θ/√n)∝n^-0.96` в этой статье не обсуждается вообще — остаётся сильнейшим живым кандидатом на новизну всего аудита; (3) DNB (PMID 22461973, открыт через PMC) — 3 критерия отслеживаются ВНУТРИ одной системы во времени, structural mismatch с H-B3-1q (сравнение MEЖДУ Peter/Paul) — НЕ переоткрытие DNB; (4) Baryshnikov 2022 (arXiv:1909.09846) — Definition 2.6 реализована в проекте корректно, Remark 2.7's предсказанный знак совпал на 2/3 озёр. Не завершены (низкий приоритет): Trefethen&Embree Ch.16 цитата, Kreiss-constant certified-accuracy алгоритмы. **[VERIFIED] §9/H-CAT31-3 (углубление главного кандидата, по прямому запросу пользователя, ЗАВЕРШЕНО):** новый Standard-Ladder эксперимент, n=32..3000 (9 точек, 40-300 реплик), pre-registered kill-criterion (CI должен содержать −1 И исключать −0.5/−2). **Результат: REJECTED для точного exponent=−1** — weighted-OLS slope=−0.9126, 95% CI=[−0.9751,−0.8501], чисто исключает −1 (не широкий неинформативный интервал). Stability-check (первая половина диапазона −0.900 vs вторая −0.897) исключает простое объяснение «finite-size коррекция к −1». Явление survives (дисперсия реально убывает степенным законом), но статус понижен с `POSSIBLE-NOVEL-SPECIAL-CASE` до `BENCHMARK-SPECIFIC-NUMERIC-RESULT` — точное число без структурного объяснения. `graph.yaml`: H-CAT31-3, статус `lead`, ребро `grounds` от H-CAT31-1 + `tested_on` к SUB-RANDOM-CIRCULANT-GRAPHS. **Честный итог всего аудита: среди ~34 проверенных claims нет ни одного подтверждённого внешне нового вклада — единственный серьёзный кандидат был испытан со всей строгостью и не выжил в чистом виде.** **CALIBRATION FIX (пользователь поймал overclaim):** формулировка «stability-check исключает finite-size correction» была сильнее данных — исправлено на «нет видимого дрейфа к −1 в диапазоне n=32..3000; простое finite-range объяснение disfavored, но асимптотика −1 за пределами диапазона НЕ исключена» (возможен `Var=C·n⁻¹·L(n)` с медленно меняющимся L(n), напр. логарифмическим). Следующий дешёвый шаг — НЕ больший sweep, а сравнение структурно-мотивированных correction-моделей на уже собранных 9 точках (не beauty contest кривых). Финальная формулировка: «robust unexplained empirical concentration law... natural n⁻¹ law falsified over the tested range».
- **[VERIFIED — 2026-09-10, ADR-122] Schema upgrade — читать вторым.** По итогам независимого аудита (context-asymmetric `Agent(analyst)`, заказан пользователем) добавлены 3 ортогональных атрибута поверх `status`: `evidence_mode` (empirical/exhaustive/deductive/formal/simulation), `verification_strength` (none/weak/medium/strong — честно снижает вес «reviewer confirmed», где это была та же модель в изолированном контексте, не другая модель/инструмент), новый тип узла `substrate` + ребро `tested_on` (делает видимой substrate-diversity: 153 узла графа концентрируются на 6 реальных субстратах). `lab_check.py` печатает substrate-diversity отчёт при каждом запуске. Retrofit ЧАСТИЧНЫЙ (15 flagship-гипотез из ~128 hypothesis/artifact узлов, честно так и помечено — см. отчёт). ADR-121's stop-rule НЕ ослаблен этим — это инфраструктура для следующего цикла, не повод его начать.
- **[2026-09-10, ADR-118 → ADR-121] CONSOLIDATION PHASE ЗАВЕРШЕНА, STOP-RULE АКТИВЕН — читать первым.** Пункты 1-2 ADR-118 выполнены: (1) B7 external novelty audit — методы вокруг H-B7-26…31 в основном устоявшиеся в литературе (не новая теория, хорошо проверенный case-study); (2) B3-1q external-data search — per-season разрез (H-B3-1r) ослабил интерпретацию H-B3-1q, красивый LEAD не выдержал робастность-проверки. Пользователь явно остановил пункт 3 (снова cross-domain/sci-hypothesis) — ADR-121. **Текущий правильный статус: НЕ «у нас ничего нет», а «есть несколько хорошо проверенных результатов, пока нет убедительно подтверждённого внешне нового открытия».** Новый цикл гипотез запускать ТОЛЬКО при появлении: (1) новой независимой модели (не Remy 2015, не Peter/Paul Lakes); (2) нового датасета (генуинно другая манипулированная/контрольная система); (3) новой теоремы/литературной щели, которую B7/B3-аудиты не нашли устоявшейся; (4) внешнего open problem с чётким structural foothold. НЕ валидное основание: «граф умеет родить ещё один H-номер». Не начинать новый `H-B*-N` без явного запроса пользователя и без одного из этих 4 условий.
- **Boundary:** Эта папка + чтение (не запись) в исходный vault `C:\Users\serge\.claude\memory\` при необходимости сверки
- **Done when:** Хотя бы один мост прошёл полный цикл kill-criterion → эксперимент → KILLED/CONFIRMED/LEAD
- **NOT NOW:** Frontier R&D / TOFT / RAF — постоянно `unverified_source` (пользователь подтвердил 2026-09-07: возможно на другой машине, доступа нет); полный Hypothesis Portfolio (128+ гипотез из ARCHCODE и др.) — отдельный, не связанный проект













## Current Focus
- **[2026-09-22 evening CHECKPOINT — H-CAT56-2 READ THIS FIRST.]** Frozen status: *exact counterexample package internally certified; portability passed via independent local reimplementation; theorem-chain read and consistent; novelty and external reproduction unresolved.* (`outreach/CHECKPOINT_FREEZE_2026-09-22.md`). **Full multi-day handoff for next agent:** `outreach/HANDOFF_2026-09-20_to_2026-09-22.md`. Three gates stay separate — object math ≠ portability ≠ novelty. Primary residual risk = theorem-context / prior art, not “is 463 wrong?”. Ordered next: (1) hand ZIP + `THIRD_PARTY_BRIEF.md` (discrepancy-first, no expected 463/21); (2) 24h cooling-off → send DRAFT-v2 per checklist (user prose needs must-fix on “general insufficiency” attribution); (3) **no new internal experiments** until U2/U3 signal. Email NOT SENT. Do not revive H-CAT31-3.
- **[2026-09-17, H-CAT31-3 Points 77-89 — long proof-attempt + measurement window on `(POL)`/`F4rel` for `CV²(y*)`; PARKED active search — do not restart without revival condition.]** Context: `(POL): sup_n E‖x*‖²<∞`, reduced (Point 85, exact KKT identity `s=2Q+1` non-degenerate-vertex support size) to `E[CV²(y)]=O(1)`. Four proof ROUTES attempted this window, all genuinely tried (not just cited) and all KILLED with a precise, quantified reason, not vague failure: (1) direct Talagrand transfer from Arora-Bhaskara's i.i.d.-edge technique — killed Point 81, circulant graphs lack the `Θ(n²)`-independent-bits structure the technique needs (re-confirmed independently Point 87§4A by two research agents); (2) uncertainty-principle-on-supports — killed Point 85, saturated with zero slack; (3) Young's/sup-norm convolution bounds on `F4rel` — killed Point 86, one reproduces the trivial bound exactly, the other's gap widens with `n`; (4) KKT/argmin-stability via LP non-degeneracy margins (`boyko-specialist`'s suggestion) — killed Point 88 after finding its cited source (Escande arXiv:2304.00809) was misattributed (real paper, wrong technique) and finding zero correlation between vertex margins and `CV²`, a result that itself SURVIVED a real adversarial skeptic pass testing 3 alternative explanations.
- **[VERIFIED-COMPUTATION, Point 87, the load-bearing measurement chain this window] `CV²(y*)` measured across 6 points, `n=127→8009`, up to 300 reps each (fewer at largest `n` — LP solve cost grew from `~0.07s`/instance at `n=509` to `~15-18min`/instance at `n=8009`, a canonical-code `45s` time-limit had to be worked around with a local no-timelimit wrapper, never touching Codex's read-only `CertificateLP`).** Two separable findings, corrected TWICE by real skeptic passes before reaching their final form (see `decision.md` Point 87 §4E-4I for the full, honest before/after): (a) **dispersion of `CV²` across independent instances shrinks as the ORDINARY, trivial `n^{-1/2}` CLT-type rate** — not a mysterious `-0.53` law as a first draft claimed; that apparent deviation from `-0.5` is entirely `mean(CV²)`'s own separate drift, confirmed via an exact algebraic decomposition (`slope(std)-slope(mean)=slope(CV_of_CV2)`, verified to 4 decimals); this dispersion law holds cleanly over about one decade (`n≈500-4000`) but is REJECTED (`p=0.0017`) across the full `127-8009` range once the endpoints are included. (b) **`mean(CV²)` is genuinely, significantly increasing with `n`** (`0.987→1.050` from `n=127→8009`, `9σ`+ from `1` at the largest `n`'s) — real, not noise — but whether it converges to a constant (`≥~1.09-1.13` at 1-2σ, no upper bound established) or grows without bound is **UNRESOLVED and likely UNRESOLVABLE within this `n`-range**: a 5-model comparison (Point 87§4I) found the candidate functional forms (log-linear, log-log, `n^{-1/4}`, `1/ln n`, `n^{-1/2}`) are nearly collinear over `n∈[127,8009]` (`|corr|` up to `0.996`), so model selection there is close to uninformative, and — critically — whichever model "wins" flips entirely depending on whether the single point `n=127` (already independently flagged, twice now, as the one point driving nearly all discrimination and a likely pre-asymptotic outlier) is included or excluded.
- **[VERIFIED-COMPUTATION, Point 89] A second, independent attempt at explaining `mean(CV²)`'s drift — the "D2" pair-weight `Dirichlet(2,...,2)` heuristic (imported from an earlier, different project branch; the user explicitly disclosed the provenance gap mid-session per Gate 1 discipline, closed here) — does NOT explain it either, but MORE importantly turned out to be algebraically CIRCULAR with the already-open `mean(CV²)` question: `R=(2-μ_D2)+(CV²-1)` exactly, so this residual test is the SAME open question in different notation, not independent evidence. A first-draft "33σ, residual converges to ≈0.085" headline was WITHDRAWN (not merely caveated) after a real skeptic pass found the specific functional form fit (`1/Q`) actively contradicts D2's own algebra (`y0·√Q≈0.5` is empirically constant, meaning the model's own leading correction is `Q^{-1/2}`, not `Q^{-1}`) and that alternate, equally-natural forms give an asymptote CONSISTENT WITH ZERO. What survives: `R>0` significantly at every tested `n` (D2 alone doesn't fully explain the energy) — a real but narrow qualitative finding.
- **[METHODOLOGY LESSON, `[REPEAT]`-worthy — happened ~9 times this window, not once]: every substantive numeric point sent to a REAL, context-asymmetric `Agent(skeptic)` (not self-review) got at least one genuine, load-bearing correction — never a rubber stamp.** Concrete catches: Point 87's first "deceleration" claim (falsified, replaced by a cleaner `~1/√n` finding); Point 87's "out-of-sample confirmation" framing at the 5th point (shown to be a low-power test); Point 87's 6th-point analysis (caught a real arithmetic error — comparing increments across intervals of different octave-width — and corrected an overclaimed "not evidence of leveling off"); Point 87§4I's entire "log-linear preferred" conclusion (shown structurally invalid — saturating models are a continuum converging to unbounded as `α→0`, and the ranking fully INVERTS without `n=127`); Point 89's "33σ" headline (withdrawn, shown to be a functional-form artifact plus algebraic circularity with already-known results). Every correction was ALSO independently re-derived by the primary session (not accepted from the skeptic's own arithmetic) before being written into `decision.md` — this dual discipline (real adversarial agent + independent re-verification of ITS claims too) is what caught issues neither self-review nor a single skeptic pass alone would likely have caught.
- **[FINAL 2026-09-18] Status at window close: `(POL)` proof-attempt line (Points 77-92) formally PARKED (Point 93) — `(POL)` and `F4rel` remain NOT PROVEN, but not disproven either.** Point 90's own two named "cheapest next steps" were both attempted and both killed/corrected by real skeptic review: Point 91 ("vertex-support universality" toward `(SP)`) KILLED — composite-modulus test-design error (`n=63`) plus a numerical threshold bug (a claimed sub-Tao-floor vertex at `n=251` was an artifact; at a clean threshold, support matched the already-known floor exactly), both independently re-confirmed with fresh code. Point 92 (`mean(CV²)` drift vs. `O(log³n)`) — the claimed "novel Parseval connection" was FALSIFIED as novel (already explicit, verbatim, in this project's own prior derivation file, `CROSS_TURAN_ENERGY_THEORY.md`); a simpler, corrected bound survives (`CV²≤n‖y‖²−1≤C²log³n−1`) but adds no new leverage. Per the pre-agreed plan's own stated contingency ("if no new inequality found, PARK"), this triggered formally: full Kill Analysis + revival condition in `parked/H-CAT31-3-pol-proof-attempt.md` + `parked/INDEX.md` row. **Deliverable of the whole 77-92 arc: 4 independently-killed proof routes, a formally-closed model-comparison line, a genuine clarification of this project's own relationship to Bandeira et al.'s published open problems, and the single most load-bearing methodology finding of the entire window — every substantive new numeric/literature claim sent to a real skeptic found at least one genuine error, zero exceptions across ~12 instances.** This does NOT park the broader `H-CAT31-3` experiment (`F4rel`, `R_n`, composite `n` remain open) or, most importantly, this project's own ORIGINAL target `Var(log(θ(G)/√n))=O(1/n)` — untouched by this detour's parking.
- **[2026-09-18, Point 94 — corrects the previous bullet's "potentially stronger-implying" framing.]** The only real derivation in this project connecting `Var(X_n)` to the mean gap (`decision.md:215-230`, self-complementarity at `p=0.5`, `E[X_n]=0` exact, `cosh(x)≥1+x²/2`) gives `V_n≤2(E[θ]/√n−1)` — the OPPOSITE direction from "`Var(X_n)=O(1/n)` gives sharp Conjecture 1." The converse is nowhere derived. Via `cosh`'s Taylor expansion (`sympy`-verified), the missing piece is a 4th-moment/tail condition on `X_n` itself — structurally the same kind of gap already known for the parked `(POL)` route, and structurally analogous to (but NOT the same object as) this project's own `F4rel` quantity (a different, per-coordinate ratio). `Var(X_n)=O(1/n)` remains this project's own valid, still-open original target — it just isn't a documented shortcut to Bandeira et al.'s sharp constant. **Addendum, same day, independently sympy-verified: a rare-value construction (`X_n=±2log(n)` w.p. `1/(8n(log n)²)` each) has `Var(X_n)=1/n` EXACTLY yet `E[cosh(X_n)]→∞`** — showing the missing condition can fail arbitrarily badly (mean gap diverging), not just miss the sharp constant `1`.
- **[2026-09-18, H-CAT31-2 continuation]** Tested this experiment's own Revival Condition (continuous divisor-feature `d(n)` vs tail structure, 17 non-cherry-picked `n∈[81,567]`). Literal pre-registered criterion gave `LEAD` (`Spearman(d(n),var_X)=-0.79`) — but a mandatory confound check (`Spearman(n,d(n))=0.77`; `d(n)`'s own coefficient in `log(var_X)~log(n)+d(n)` is `-0.005, p=0.64`, incremental `R²=0.0003`) shows this is entirely explained by `d(n)` tracking `n` itself, not an independent effect. **Corrected final verdict: `REJECTED`.** Revival now needs `n` held fixed while `d(n)` varies (design-based decorrelation), not a wide-range sample where the two co-vary by arithmetic necessity.
- **[2026-09-18, Point 95 — R_n follow-up, same pattern one more time (`[REPEAT×13]`).]** Ran Point 53's own pre-registered next step (tightened reps for `R_n`). Raw result looked encouraging (`n·R_n`'s CI clears zero at `n=509,1021`; small-n power law "fails" at 2 points now) — sent to a real skeptic BEFORE writing anything, all 4 draft conclusions found unsupported (3 `FALSIFIED`, independently re-verified): `n·W_1` is an exact algebraic function of `λ_n` alone (no new info); the "power-law failure" is a rep-count/CI-width artifact — a properly weighted exponent fit gives CI `[-0.316,0.284]`, comfortably containing `0.239`; the `n·R_n` "trend" (`r²=0.988` unweighted) is a well-fit CONSTANT once weighted (`χ²=0.097, p=0.95`) — the weighted scaling-exponent CI `[-0.79,1.09]` contains BOTH a true plateau AND `Var(X_n)` not decaying as `1/n` at all; `R_n≥0` is a sum-of-squares identity, "confirmed real" was never actually open. **The scaling question `R_n` was meant to answer remains fully open** — not confirmed, not refuted, essentially untouched by 4-8× more reps at only 3 unevenly-weighted points. Concrete revival plan named (combine old+new data; equalize rep counts; reformulate as a CI on the exponent `b` with a pre-registered stopping rule to exclude `b=1`; BCa bootstrap for the skewed boundary-sensitive CI).
- **[FINAL 2026-09-18] User-confirmed decision after Point 95: PARK ACTIVE SEARCH AFTER POINT 95 — the entire `H-CAT31-3` active-search effort (not just the `(POL)` proof-attempt line), extending Point 93's PARK.** Rationale, user's own analysis, independently agreed after re-checking every cited number against this window's own findings: `F4rel` has no independent route (its clean path to the main theorem needs the already-parked `(POL)`'s tighter second-moment bound — Point 92's own algebra); `R_n`'s Point 95 follow-up leaves the scaling question exactly as open as before (weighted CI contains both `b=0` and `b=1`); composite `n` was separately tested and REJECTED by H-CAT31-2's own confound-controlled continuation (not merely parked). No further ready, non-tautological cheap next step remains for this experiment. **This is a deliberate research PAUSE, not a kill — explicitly NOT manufacturing a "Point 96" merely to keep producing output** (user's own words: not producing a new point just because the numbers on the keyboard haven't run out). Documentation: `parked/H-CAT31-3-pol-proof-attempt.md` retitled/rescoped to cover Points 77-95 (added a `(d)` revival sub-condition for `R_n` specifically — Point 95's own 4-step skeptic-named plan) and `parked/INDEX.md`'s `H-CAT31-3-POL` row updated to match. Does NOT touch: this project's own original target `Var(log(theta(G)/sqrt(n)))=O(1/n)` (remains open, per Point 94's correction — no longer a documented shortcut to Bandeira et al.'s sharp constant, but not disproven); the broader `H-CAT31-3` graph node's existing REJECTED verdict for the earlier, narrower exponent claim (~Point 54, untouched). **Next step is explicitly open per ADR-087's scope-expansion mandate — not yet decided, to be posed to the user rather than unilaterally started.**
- **[2026-09-18, H-CAT30-1 — new catalog hypothesis (ADR-087), REJECTED on its first pilot, same-day.]** User chose to pursue catalog item #30 (Sharp tensor concentration inequalities) after a background `Explore` agent screened ~81 unscreened catalog items. Source verified directly (not from the catalog's own templated paraphrase): Bandeira et al., `arXiv:2603.29571`, Entry 8, Conjecture 16 ("Type-2 constant of Tensors") — proven for `p≥2r`, OPEN for `p<2r`. Chose `r=3,p=2` (the sharpest open sub-case: conjectured rate is dimension-FREE, `d^0`). Pilot: fixed `n=20` random rank-1 tensors, Monte-Carlo-estimated `E||Σg_iT_i||_{I_2}` across `d∈{5..160}`, found an apparent flat plateau at large `d` — looked like real evidence consistent with the conjecture. **Sent to a context-asymmetric skeptic BEFORE writing anything (per this project's own now `[REPEAT×14]` discipline) — FALSIFIED.** The skeptic proved the pilot design is mathematically DEGENERATE: `f(x)=Σg_i⟨a_i,x⟩^3` depends on `x` only through `u=Ax∈R^n`, so the optimum is confined to `span{a_i}` (≤n=20 dimensions) regardless of `d` — the "plateau" was `E[max_i|g_i|]≈2.167` (an `n`-dimensional order statistic), not evidence about the tensor's dimension at all. Independently re-verified twice before accepting: closed-form asymptotic matches the observed plateau to 0.48%; optimizer's `x*` lies EXACTLY in `span{a_i}` at every tested `d` up to `d=400` (residual `~1e-16`). Conjecture 16 itself remains untouched/genuinely open — only this specific fixed-`n` construction's ability to probe it is killed. Full writeup: `null_results/H-CAT30-1-tensor-concentration-fixed-n-degenerate.md`, revival path (n=n(d) scaling) named. Numerical pipeline itself verified correct and reusable (5/5 pytest, ruff clean).
- **[2026-09-18, H-CAT30-2 — implemented H-CAT30-1's own revival redesign, REJECTED same day for a DIFFERENT mechanism.]** User said "го все по очереди" (work through all queued options). Before pursuing option 2 (#43), checked `CLAUDE.md` itself and found #43 already invalidated in this project's own history (ADR-084, `.claude/memory/decisions.md`): the primary source (`arXiv:2602.05394` v3) already documents a NEGATIVE resolution (counterexamples, Townsend & Wang `arXiv:2604.10215v2`) — the 100-item catalog's "Open" label for #43 is stale, confirmed 2026-09-09, before this session. A prior screening agent this session missed this and wrongly recommended #43 — caught by checking CLAUDE.md's own NEVER section before acting, not by trusting the agent. #43 skipped; queue reordered to redesign #30 first, then #33. Redesign (H-CAT30-2): implemented the exact named fix (`n=n(d)` scaling, two independent scales `n=d` and `n=d^1.5`), independently confirmed `rank(A)=d` at every point (span-confinement structurally impossible now). **Sent to skeptic before writing anything — FALSIFIED again, different mechanism.** The Gram matrix `G=AA^T` concentrates toward `I_n` for ANY isotropic random rank-1 family; at `G=I`, `||S||_{I_2}=max_i|g_i|` EXACTLY (a KKT-provable closed form) — predicted `β=-0.376/-0.585` (zero fitted params) matched observed `-0.3673/-0.5613` to 2.5-4%. Independently re-verified: exact recovery (`0.0000%` deficit) on an orthonormal canary at `n=80`, confirmed `run.py` never actually calls its own claimed `scipy_cross_check` (dead code, real methodology gap), confirmed the "two independent scales" were arithmetically forced to have `β_B/β_A=1.5` (observed `1.528`) — not independent tests at all. **Kill Analysis broadened retroactively**: the entire "isotropic random rank-1 tensor family + `sqrt(Σ‖T_i‖²)` normalization" approach is now killed across two independently-discovered mechanisms, not just one design. Stopping active search on H-CAT30 here (two full experiments, two skeptic rounds, same discipline as H-CAT31-3's PARK) — revival needs a genuinely different construction (structured/coherent-frame tensor family or real adversarial search), not another `n(d)` variant. Full writeup: `null_results/H-CAT30-2-tensor-concentration-gram-concentration-foreordained.md`. Moving to queue item #33 next.
- **[2026-09-18, #33 (Helly/Steinitz) — STOPPED on source access, not pursued.]** [VERIFIED] DOI resolves, paper real (Naszódi, Analysis Mathematica 2025); [VERIFIED] full text is paywalled despite "open access" label (Springer auth-wall); [VERIFIED] no arXiv mirror or author-homepage preprint found after a real search. Per this project's own Source Trace discipline (never build on a catalog paraphrase — the exact lesson from #43's staleness), did NOT proceed on the templated catalog description. User chose to stop for the day rather than substitute a lower-confidence candidate.
- **[2026-09-18, H-CAT31-3 revival: user relayed an external atomization of headline claim `C0` (`V_n=O(1/n)`) into 5 sub-claims C1-C5.]** Cross-checked against this project's own history BEFORE running anything (Adaptive Iteration Branch Rule) — found the relayed atomization significantly understated prior work: **C1/C2/C3** (`λ_n`/`R_n`, jointly `⟹C0` / `sup|λ_n|=∞⟹¬C0`) are literally the `R_n` scaling question PARKED earlier the SAME session (Point 93/95) — not new. **C4** (`K_n=O(1)`, proven-sufficient-not-necessary) has an unacknowledged 22-point history (Points 56-78), already reduced to "empirical channel EXHAUSTED" (Point 77) and the same joint-moment (`J_n`/MIX4) target the parked `(POL)` line pursued. **C5** (`g_n=O(n^{-1/2})`, mean-gap) was the one genuinely untested route. User confirmed: execute C1/C2/C3's own already-named revival plan; for C4 run only the one concretely-missing cheap check (Point 76's own flagged-but-skipped `n=127` leverage robustness for `K_n`); C5 gets a genuinely new test.
  - **Point 96 (C4):** ran the missing `K_n` leverage check. Drafted "robust to n=127 exclusion" — **sent to skeptic, FALSIFIED as an overclaim**: full leave-one-out (all 4 exclusions, not just n=127) gives `b` always positive (`0.051-0.094`) but never significant; the test was foreordained-uninformative (needed `33%` growth in K_n to detect anything, observed only `12%` — independently caught and CORRECTED a real arithmetic error in the skeptic's own report, which had claimed `78%`). Corrected finding: uninformative, not robust. `K_n=O(1)` remains open.
  - **Point 97 (C5):** first draft found `g_n`'s fitted decay exponent statistically excluded the needed `-0.5` by `>3σ` — looked like a real, concerning finding. **Sent to skeptic, FALSIFIED at the root**: `g_n/√n = V_n/2 + O(X⁴)` via cosh's own Taylor expansion (already in Point 94's derivation) — `g_n`'s exponent is ALGEBRAICALLY forced to `≈0.5+b_V`, not an independent quantity; confirmed numerically to `<1.5%` precision. Separately: the 9-point dataset used contains ZERO prime `n` (all composite/powers-of-2) — the headline theorem's own machinery is specifically about prime `n`. C5's empirical channel gives no independent evidence; the theoretical route (an independent, non-Monte-Carlo bound on `E[θ]`) remains open in principle.
  - **Point 98 (C1/C2/C3):** executed Point 95's own 4-step revival plan in full — pooled old+new draws, equalized to `800-1000` reps at `n=509/1021/2039` (`~159min` LP-solve at `n=2039` alone), BCa+nested bootstrap. First draft: CI excluding both `0` and `1` — looked like the most concerning finding of the day. **Sent to skeptic, FALSIFIED**: delta-method log-SE bug (should have used the already-equivariant BCa log-interval) — corrected, CI is `[-0.04,1.01]`, back to `STILL INCONCLUSIVE`, same bucket as Point 95 even at `5×` the sample size (leave-one-out confirms: not robust either). **But the SAME pooled data, fit DIRECTLY on `n·Var(X_n)` instead of through the `R_n` decomposition, gives `b_Var=-0.042±0.046`, 95% CI `[-0.13,0.05]`** — `5×` more precise, and the single most informative, most positive confirmatory number for `C0` this entire project has produced. `[REPEAT×16+]`: every one of 3 dispatches today found a real, load-bearing error in a positive-leaning draft — one of which was MY OWN correction of the skeptic's own arithmetic mistake, underscoring "never trust any single pass, human, model, or agent, at face value."
  - Full details: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/decision.md` Points 96-98; `parked/H-CAT31-3-pol-proof-attempt.md` and `parked/INDEX.md` updated with the executed revival outcome and a new, sharper revival condition (`n≈8191`, pre-registered `~3.5σ`-differentiating kill criterion).
  - **[2026-09-18, Point 99 — epistemic-status correction to the above, prompted by external review.]** "Skeptic" in Points 96-98 above means same-model, context-asymmetric (no cross-model channel wired in this repo, per `pearl_registry/INDEX.md`'s 2026-07-19 row) — correct label per `rules/falsification-ladder.md`'s own Independent Verification Strength Ladder is `INTERNALLY CROSS-CHECKED` (Weak-Medium), not `INDEPENDENTLY_VERIFIED`. The 3 corrected findings are unaffected (independently re-derived with fresh code before acceptance either way); only the strength label. Also: `b_Var=-0.042±0.046` (Point 98) is supporting empirical evidence consistent with `C0` at 3 `n`, not proof of the asymptotic rate. One-line freeze: **Main claim `C0` OPEN. C1-C3 revival executed, non-discriminating, re-PARKED. C4 claim OPEN/empirical channel exhausted. C5 claim OPEN/tested route invalid as discriminator.** `[UPDATE]` Points 96-99's work was pushed to `origin/main` (`fed6520..08fce7c`) after live confirmation — `REMOTE-SYNCED`, not merely local.
  - **[2026-09-18, Point 100 — `boyko-specialist` dispatch on C5's own revival condition (a), user's explicit choice.]** Searched for a non-empirical upper bound on `E[θ(G_n)]` via LP duality/union-bound. **Found the exact literature this route needed** — Bandeira, Błasiok, Dmitriev, Faure, Kireeva & Kunisky 2025 (already central to this project), Theorem 1: `E[θ(G)]≤C√(n·log log n)`, `[VERIFIED]` via direct primary-source fetch. **But independently re-derived: plugging it into `V_n≤2g_n/√n` (Point 94) gives an `O(√(log log n))` bound that GROWS with `n`** — `163×`-`17372×` too loose (illustrative `C=1`) vs the `O(1/n)` target across `n=509..10⁵`, even under the field's own best-case still-OPEN sharp Conjecture 1 as literally stated. Full derivation: `experiments/20260910-lovasz-theta-variance-scaling-cat31-3/decision.md` Point 100, artifact `c5_bandeira_bound_check.py`.
  - **[2026-09-18, Point 101 — correction to Point 100, prompted by external review.]** Two real errors caught: (1) citation misattribution — Point 100 said "Bandeira & Ferber," but no author named Ferber exists on that paper (real 6-author list above); this project's own EARLIER text already correctly named Ulysse Faure as co-author, which should have caught the error. (2) Point 100's verdict overclaimed — "unreachable via any general `E[θ]` bound, proven or conjectured" is too strong. What was actually shown: currently PUBLISHED (Theorem 1) and currently CONJECTURED-without-a-rate (Conjecture 1 as literally stated) results are insufficient — real, unaffected finding. But a FUTURE result (proven, or conjectural WITH an explicit rate) establishing `E[θ(G_n)]/√n=1+O(1/n)` specifically is NOT ruled out by anything shown. **Corrected: revival condition (a) is SHARPENED (to this specific `O(1/n)`-relative target), not CLOSED as impossible.**

[summarized] - **[VERIFIED, 2026-09-19] `/boyko-capability-audit` run on tooling-methodology (HYPOTHESIS:...

## Project State
- **Repo:** https://github.com/sergeeey/Y-17-100-gipotez — PUBLIC, created 2026-09-06, commit d50597f (initial import). [VERIFIED]
- **Excluded from public repo (.gitignore):** `H-7 GeoSpectra Lab (DIFFERENT project…).md` — third-party correspondence refs (Tom Lawrence). Local copy kept. User may override.
- **New meta-goal (2026-09-06, user):** make this an *exemplary experimental lab* — memory/structure/hypothesis-graph designed up front so a blind orchestrator can operate; use it to test the whole tool/agent/methodology stack. **Variant C chosen by user (AskUserQuestion) → ADR-002.** Every tool use → row in `tooling-eval/LEDGER.md` (25 rows after session 1: CAUGHT 6 / OK 8 / NOISE 5 / NOT-YET 6).
- **Graph validator:** `python scripts/lab_check.py` (SCHEMA invariants 1–3) + `pytest` (3 tests incl. negative control replicating the 2026-05-28 incident). Run both before every commit.
- **FL template source (reuse, don't reinvent):** `D:\Claude-cod-top-2026\experiments\_template\` (14 files) [VERIFIED]
- **Files transferred:** 15 (2026-09-06)
- **Bridges scoped:** 3, все терминальны (2026-09-09): RMT/Riemann — Phase 1a READY, 1b BLOCKED (external Option A); ChernoffPy/UDE — CONFIRMED-WITH-CAVEATS, арка H-B2-1→1v закрыта 2026-09-08; May1972/TDA — CLOSED 2026-09-09 как informative negative (0 confirmed / 8 killed / 1 parked из 15 под-гипотез), арка H-B3-1→1p. **[2026-09-10] H-B3-2** (новая статья, PH₀ chirality-excess, route 3 отчёта) добавлена к той же закрытой Bridge 3 — REJECT после bug-fix-and-rerun, не реоткрывает мост (по-прежнему 0 confirmed на этой линии); Mechanism Claim Gate внутри неё — единственный устоявший позитивный побочный результат.
- **Bridges permanently `unverified_source`** (answered 2026-09-07, not pending): 3 (Frontier R&D, TOFT/SMT, RAF Theory)













## Architecture (файлы этой папки)
- `00-catalog/` — источники задач (raw + verified subset + skeptic assessment)
- `01-cross-domain-bridges/` — главный рабочий файл + H-7 контекст (два разных проекта!)
- `02-related-projects-context/` — ChernoffPy, May 1972
- `03-methodology-rules/` — переиспользуемые правила (execution rules, submission gate, ESV scoring)













## Quick Commands
```bash
pip install -r requirements.txt
python scripts/lab_check.py          # graph invariants (must be OK before commit)
python -m pytest -q                  # 3 tests
python -m ruff check scripts/ tests/ # lint (line-length=100 pinned in pyproject.toml)
# LEDGER summary — count by grep, never by hand:
grep -E '^\| (2026-[0-9-]+|—) \|' tooling-eval/LEDGER.md | awk -F'|' '{gsub(/ /,"",$6); print $6}' | sort | uniq -c
```













## Open Questions (для пользователя)
1. ~~Frontier R&D / TOFT / RAF Theory — реальны на другом компьютере, или нет?~~ **[VERIFIED —
   прямая цитата пользователя, 2026-09-07, эта сессия]:** "на другой машине может и раньше были
   но сейчас у меня нет доступа к этой машине". Это ПОСТОЯННОЕ, не временное состояние — B4-B6
   остаются `unverified_source` в graph.yaml без дальнейшего перефлагирования как открытого
   вопроса каждую сессию. Не строить на них выводы, но и не переспрашивать снова без новой
   информации от пользователя.
2. Доступен ли этот E:\ путь с других ПК (тот же физический диск / сетевая шара / нет)?

---
*Создан: 2026-09-06 при переносе из Obsidian vault.*













## Auto-commit log
- [2026-09-20 18:04] `c4f347c` (local, branch `feature/small-exact-instance` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Pre-register A'' prefix-resampling search; A and A' results (both FAIL by criteria, best rank 456 of 463)
- [2026-09-20 18:01] `8aa060e` (local, branch `feature/small-exact-instance` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Pre-register A' follow-up search (grid, budget, outcomes) before running; exploratory results and search scripts
- [2026-09-20 17:54] `f8c365a` (local, branch `feature/small-exact-instance` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Pre-register: small explicit Gaussian-integer instance search (criteria, budget, stop rules) before any run
- [2026-09-20 17:19] `65c8170` (local, branch `feature/outreach-drafts` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Scope correction: matrix and scalar QCRB saturation coincide for positive-definite weights
- [2026-09-20 17:12] `97dd1af` (local, branch `feature/outreach-drafts` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Outreach: lemma packet v2 (H0 + editorial fixes) and record of the internal blind pass
- [2026-09-20 17:11] `b6aedce` (local, branch `feature/outreach-drafts` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): Outreach: authors email draft (not sent), pre-registered outcome criteria, blind lemma packet v2 + record of internal blind pass
- [2026-09-20 17:00] `484081b` (local, branch `h-cat56-2-verification-gates` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): H-CAT56-2 verification gates: published-example control, spectrum stability, threshold table, lifting lemma, null-outcome check against source, novelty audit, goal-expansion report
- [2026-09-19 15:55] `b9af208` (local, branch `docs/prior-result-gate-h-cat56-3` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): docs: Prior Result Gate for H-CAT56-3 (minimal POVM size), not registered
- [2026-09-19 15:45] `09d1a4a` (local, branch `docs/prior-result-gate-llm-errors` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): docs: Prior Result Gate for LLM-errors-as-hypotheses target (no H-number issued)
- [2026-09-19 14:23] `20736e5` (local, branch `feat/h-cat56-2-counterexample-h-cat31-4-primes` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): chore: auto-log commit history entries
- [2026-09-19 13:23] `2c95f49` (local, branch `feat/h-cat56-2-counterexample-h-cat31-4-primes` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): docs: Bridge 10 (LP vertex count: Lovasz theta <-> POVM size) rejected as trivial
- [2026-09-19 12:14] `8813a33` (local, branch `feat/h-cat56-2-counterexample-h-cat31-4-primes` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): feat: H-CAT56-2 certified counterexample to PCC sufficiency + H-CAT31-4 prime-n sweep
- [2026-09-19 02:04] `f621e57` (local, branch `docs/h-cat56-1-p2-cleanup-log` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): docs: log H-CAT56-1's 2 P2 reviewer findings as KNOWN-CLEANUP, not fixed
- [2026-09-19 01:55] `27e8aa5` (local, branch `fix/h-cat56-1-repeat-verdict-verified` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): fix: H-CAT56-1 -- REPEAT verdict independently verified, registered
- [2026-09-19 01:15] `f7fca4e` (local, branch `fix/h-cat7-1-kill-analysis-staleness` -- may be replaced if this branch is later merged via squash or rebase; check that branch's PR/merge for the surviving hash if this one becomes unresolvable): fix: H-CAT7-1 decision.md Kill-Analysis staleness + LEDGER.md stale summary
