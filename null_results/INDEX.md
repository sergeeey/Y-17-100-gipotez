# null_results/INDEX.md — реестр фальсифицированных гипотез

_Запись здесь означает: утверждение проверено и **ОПРОВЕРГНУТО**. Не повторять без принципиально другого подхода._
_Перед любой новой гипотезой: `grep -i "<ключевое слово>"` по этому файлу._

## Как добавить запись

При вердикте REJECT в `decision.md`:
1. Скопировать заполненный `decision.md` в `null_results/<id>-<slug>.md` (Kill Analysis обязателен: что убито / что НЕ убито / Relaxation Map)
2. Добавить строку в таблицу
3. **Ретроскан:** в `registry/graph.yaml` найти все узлы, до которых от убитого ведут `grounds`/`depends_on`, пересмотреть их статус в ту же сессию
4. Обновить статус узла в `registry/graph.yaml` → `killed`

## Index

| id | date | slug | verdict | why falsified (≤10 words) | retroscan done |
|---|---|---|---|---|---|
| H-B3-1c | 2026-09-06 | lakes-tda-ews-surrogate-null-v1 | REJECT | AR(1) null too simple — real negative-control lakes have extra structure (5/5 still false-positive) | Да, на момент записи (2026-09-06, до H-B3-1d/e/f) — нет узлов, зависящих от H-B3-1c; H-B3-1/H-B3-1b (родители) не затронуты, их CRITERION_INVALID вердикты остаются в силе. **Обновление той же сессии:** H-B3-1d (grounds) и H-B3-1f (grounds, descriptive join) позже сослались на H-B3-1c как на предка — это ОЖИДАЕМО (Relaxation Map/повторный анализ), не отменяет REJECT, но делает эту графу устаревшей как снимок «нет зависимых узлов» |
| H-B3-1d | 2026-09-06 | lakes-tda-ews-iaaft-null-v1prime | REJECT | IAAFT (full spectrum, richer than AR(1)) still 5/5 false-positive — rules out spectral richness, points to non-stationary trend | Да, на момент записи (2026-09-06, до H-B3-1e/f) — нет узлов, зависящих от H-B3-1d; H-B3-1c не затронут, REJECT остаётся в силе. **Обновление той же сессии:** H-B3-1e (grounds) и H-B3-1f (grounds, descriptive join) позже сослались на H-B3-1d как на предка — ОЖИДАЕМО (Relaxation Map/повторный анализ), REJECT не отменён |
| H-B3-1e | 2026-09-06 | lakes-tda-ews-detrend-surrogate-v2prime | REJECT | detrend+IAAFT: та же тройка null-моделей даёт БАЙТ-В-БАЙТ идентичный набор 5/5 ложных срабатываний | Да — нет узлов, зависящих от H-B3-1e; H-B3-1d не затронут, REJECT остаётся в силе |
| H-B2-1 | 2026-09-06 | chernoff-neuralode-1d-decay | **SUPERSEDED → PROMOTE** (см. ниже) | ~~теорема Чернова о скорости даёт гарантию на 1 порядок n слабее истинной эмпирической ошибки~~ — верно ТОЛЬКО для упрощённого следствия (Theorem 1.2); главная теорема статьи (Theorem 3.1, прочитана позже в ТОЙ ЖЕ сессии) даёт точную, совпадающую по порядку границу — см. `decision.md` § CORRECTION ADDENDUM | Да — единственный потомок B2-CHERNOFF-UDE. **Эта строка исторически неверна как финальный вердикт — оставлена для аудиторской цепочки (Hindsight Distortion Gap discipline), актуальный статус узла в graph.yaml: `confirmed`** |
| H-B3-1i | 2026-09-06 | lakes-tda-ews-total-persistence-iaaft-v1gprime | REJECT | FP-набор буквально идентичен V1g (4/5); НО лид Peter doSat (+13d устойчиво в 4 вариантах) разворачивает знак на -74d | Да — H-B3-1i не имеет потомков-узлов; проверено, что H-B3-1g/H-B3-1h (LEAD) не опираются на безусловную формулировку робастности лида Peter doSat в своих формальных `decision.md`-вердиктах (только в свободной прозе `activeContext.md`, которая уже скорректирована) — их LEAD-статус не затронут, они верны в своих исходных рамках (AR(1)-null варианты) |
| H-B3-1j | 2026-09-06 | lakes-tda-ews-diagram-distance-v1j | REJECT | FP-набор идентичен V1g/H-B3-1i (4/5) даже под принципиально другой FAMILY детекции (diagram-distance, не scalar-summary) | Да — H-B3-1j не имеет потомков-узлов; уточняет H-B3-1i (не отменяет): лид Peter doSat положителен под AR(1) с ОБЕИМИ статистиками, отрицателен только под IAAFT — H-B3-1i/H-B3-1g/H-B3-1h остаются в силе в своих исходных рамках |
| H-B3-1k | 2026-09-06 | lakes-tda-ews-diagram-distance-iaaft-v1kprime | REJECT | Прямой тест собственного предсказания H-B3-1j (что IAAFT+diagram-distance даст отрицательный лид) — предсказание ОПРОВЕРГНУТО, лид +4.0d положителен | Да — H-B3-1k не имеет потомков-узлов; ребро H-B3-1i→H-B3-1k типа `grounds` (не `invalidates`): H-B3-1i's собственная измеренная величина (зашумлённый IAAFT-порог t=172d) остаётся верной, опровергнута только обобщённая гипотеза из pearl-записи H-B3-1j, не сам узел H-B3-1i |
| H-B3-1m | 2026-09-07 | lakes-tda-ews-changepoint-v2 | REJECT | Floor резолвится чисто на 42-44% (reps=500, per-lake seed — было CRITERION_INVALID при reps=30, шум); НО Loch Leven реально two-part-crosses на собственных данных (не только AR(1)-нуле) — истинный false positive на негативном контроле | Да — H-B3-1m не имеет потомков-узлов (единственное ребро H-B3-1b→H-B3-1m типа `grounds`, родитель не затронут); закрывает ПОСЛЕДНЮЮ из 3 исходных строк Relaxation Map H-B3-1b — все три теперь REJECT/CONFIRMED-с-конфаундом, ни одна не дала чистый PROMOTE |
| H-B7-4 | 2026-09-06 | remy-tumorigenesis-transient-h4 | REJECT (для do(RAS=1)) | Транзиентная активация RAS (1-30 шагов) в bistable-ветке реальной модели рака мочевого пузыря не смогла перевести систему из Growth_arrest в Proliferation, несмотря на подтверждённую мультистабильность ветки | Да — H-B7-4 является потомком H-B7-3 (ребро `grounds`, продолжение Relaxation Map), сам не имеет потомков; в отличие от H-B7-3 (TASK_INFEASIBLE), это ГЕНУИННО информативный REJECT — структурная предпосылка успеха существовала (подтверждена `pyboolnet`), тест мог бы подтвердить гипотезу, но не подтвердил для этого конкретного возмущения |

| H-B7-5 | 2026-09-06 | remy-tumorigenesis-twohit-h5 | REJECT (для do(RAS=1, TP53=0)) | Комбинированный RAS+TP53 (стандартная кооперирующая пара) снова недостаточен — p21CIP держится через TP53-независимый Growth_inhibitors-путь | Да — H-B7-5 является потомком H-B7-4 (ребро `grounds`, продолжение Relaxation Map), сам не имеет потомков; на этот раз REJECT сопровождается прямо прослеженным механизмом (не гипотезой), а не просто повтором отрицательного результата |

| H-B7-6 | 2026-09-06 | remy-tumorigenesis-threehit-h6 | REJECT (для do(RAS=1, TP53=0, p21CIP=0)) | Growth_arrest — явный тройной OR (p21CIP\|RBL2\|RB1); CyclinD1 доказуемо мёртв в этой ветке (алгебра + pyboolnet на всех 3 реальных аттракторах) | Да — H-B7-6 является потомком H-B7-5 (ребро `grounds`, продолжение Relaxation Map), сам не имеет потомков; на этот раз REJECT сопровождается механизмом, верифицированным двумя независимыми способами (не гипотезой), объясняющим разом провал H-B7-4, H-B7-5 и H-B7-6 |

| H-B2-1q | 2026-09-07 | omega-abscissa-large-n-fixedgrid | REJECT (hard_killed, see Revival Condition) | N=64 pre-registered fixed-grid replication (fresh seeds): rho=0.073,p=0.579 — H-B2-1p's rho=0.298,p=0.021 was drawer-selection | Да — H-B2-1q не имеет потомков-узлов (единственное ребро H-B2-1p→H-B2-1q типа `grounds`, родитель не затронут — H-B2-1p's MIXED вердикт уже был корректно ослаблен собственным skeptic-проходом до фиксации, этот эксперимент лишь напрямую подтвердил то, что skeptic предсказал аргументом). Закрывает всю численно-абсциссную под-арку B2 (H-B2-1m→1n→1o→1p→1q): твёрдо при N∈{16,32}, генуинный ноль на 6 значениях N от 40 до 80 на 2 независимых диапазонах сидов |
| H-B3-1l | 2026-09-08 | lakes-tda-ews-peaktau-v3 | REJECT (opposed to CONFIRMED, via H-B3-1p) | Symmetric classical-peak selection (same rule as negative controls) picks AC1 (1999.25), TDA (2000.5) is LATER not earlier — original +50.0mo "TDA leads" lead was an artifact of oracle-informed statistic selection, inverts to -15.0mo | Да — H-B3-1l имеет 2 потомка (H-B3-1n grounds, H-B3-1p grounds/invalidates). H-B3-1n's собственная AR(1)-суррогатная проверка (INCONCLUSIVE) остаётся валидной записью о том, что она тестировала, хотя целевое число (+50.0mo), которое она проверяла, теперь известно как несправедливо выведенное с самого начала — не отменяет H-B3-1n's методологию/тесты, только целевой параметр. Peter doSat's независимый +13-месячный лид (4× реплицирован через V1/V1'/V1g/конъюнкцию) НЕ затронут — полностью независим от проблемы отбора статистики Lower Zurich |

| H-B2-3 | 2026-09-10 | transferable-dimensionless-predictor | REJECT | Fitted power-law transfers ~2x on new T, but ~200x worse on new matrix family+T (order-of-magnitude threshold) | Да — H-B2-3 не имеет потомков (новый лист арки B2). H-B2-2 (родитель, grounds) не затронут — его собственный вывод (K_ref рыхло ограничивает M1, efficiency 3.7-31%) не оспаривается, это другой вопрос (transferability, не tightness) |
| H-B3-2 | 2026-09-10 | lakes-tda-ews-ph0-chirality-directed | REJECT | Bug-fix-and-rerun falsification: PH0 chirality-excess CONFIRMED result relied on a buggy `merge_tree_bars` (silently dropped global min on ~55% of windows, caught by independently-invoked reviewer via hand-built counter-example); corrected statistic fires false alarm on Windermere and has the WORST floor FPR of 3 tested statistics | Да — H-B3-2 не имеет потомков-узлов (новая ветвь Bridge 3, отдельная от H1-VR арки H-B3-1..1p). Mechanism Claim Gate (H1-VR доказуемо слеп к развороту времени, 5/5 испытаний) НЕ затронут этим REJECT — оценивался отдельно и остаётся в силе. H-B3-1b (родитель, grounds) не затронут — данные/инфраструктура переиспользованы без изменений |
| H-B7-13 | 2026-09-10 | remy-tumorigenesis-observability | REJECT | Exact collision search (80/80 release-states, both branches, k=1..40): neither `{Growth_arrest,Proliferation}` nor `+{p21CIP,RBL2}` distinguishes the safe (k=3,4) from unsafe (k=5) release decision -- markers lag the committed fate by ~1 step at exactly the threshold | Да — H-B7-13 не имеет потомков-узлов (новая ветвь Bridge 7, наблюдаемость release-decision, отдельная от attractor-existence арки H-B7-1..12). Floor/ceiling sanity checks оба прошли (детектор не тривиален); H-B7-10/H-B7-12's k*=5 threshold не затронут — эксперимент проверяет ЛЕГИБИЛЬНОСТЬ порога из маркеров, не сам порог |

| H-B2-4 | 2026-09-10 | multiseed-permutation-nonmonotonicity | REJECT | Direct execution of H-B2-1k's own named fix (multi-seed x permutation-test criterion, replacing a single-seed sign-change test found CRITERION_INVALID). 30 seeds x 9 N_DIM: seed-averaged M1(N_DIM) is PERFECTLY monotonic (0 sign changes), p=1.0 vs a 2000-permutation null (mean 4.64 sign changes) -- a clean, confident REJECT, not another inconclusive floor-defect result | Да — H-B2-4 не имеет потомков-узлов (лист арки B2). H-B2-1k's own raw M1 measurements и H-B2-1l's eigenvector-conditioning hypothesis не затронуты (другой вопрос); H-B2-1j's основной подтверждённый claim (Theorem 3.1 bound valid/order-matching at N=50) не затронут — опровергнута только его отдельная pearl_registry falsifiable prediction (non-monotonic M1 profile) |

| H-B3-1r | 2026-09-10 | crossvar-coherence-season-robustness | REJECT | Robustness check on H-B3-1q's own pooled cross-variable tau-trajectory coherence: does the pattern replicate per individual field season (2008/2009/2010) instead of pooled? FAILS both pre-registered bars -- 2008 ties Peter-vs-Paul (1/3 each) instead of favoring Peter, and Peter's own count DROPS from 3/3 (2009) to 2/3 (2010, the documented transition-completion year) instead of strengthening. FL Step 8a reviewer independently reproduced both load-bearing numbers exactly, incl. the sign-critical 2010 value | Да — H-B3-1r не имеет потомков-узлов. Kills only the "genuine season-independent, intensifying signal" INTERPRETATION of H-B3-1q's pooled pattern -- H-B3-1q's own pooled correlation numbers remain correctly computed and unaffected (status stays `lead`, not `killed`); H-B3-1o's original delay-vs-artifact question unaffected, stays `parked` |

| H-CAT30-1 | 2026-09-18 | tensor-concentration-fixed-n-degenerate | REJECT | Pilot testing Bandeira et al.'s open Conjecture 16 (`arXiv:2603.29571`, tensor concentration, `r=3,p=2` open regime `p<2r`) via a fixed-`n=20`, random-rank-1 scan over `d` -- structurally DEGENERATE: `f(x)=Σg_i⟨a_i,x⟩^3` depends on `x` only through `u=Ax∈R^n`, so the optimum is confined to `span{a_i}` (≤n-dim) regardless of `d`. Independently re-verified twice: closed-form asymptotic `E[max_i\|g_i\|]=2.1667` matches the observed "plateau" (2.1565) to 0.48%; optimizer's `x*` lies EXACTLY in `span{a_i}` (residual `~1e-16`) at every tested `d`, incl. `d=400`. The pilot's own kill criterion was derivable in 3 lines before running any code -- not a test. Conjecture 16 itself untouched (genuinely open); only this specific fixed-n construction's ability to probe it is killed | Да — brand-new node, no dependent nodes at the time of this REJECT. **UPDATE 2026-09-18 (same day)**: the named revival redesign (`n=n(d)` scaling) was attempted same-day as H-CAT30-2 and ALSO REJECTED, for a genuinely different mechanism (Gram-matrix concentration to `I`, not span-confinement) -- see `H-CAT30-2` row below. Kill Analysis retroactively broadened: the entire "isotropic random rank-1 tensor family + sqrt(Σ‖T_i‖²) normalization" approach is killed across both mechanisms, not just this specific design |
| H-CAT30-2 | 2026-09-18 | tensor-concentration-gram-concentration-foreordained | REJECT | Redesign of H-CAT30-1 implementing its own named revival condition (`n=n(d)` scaling, two scales: `n=d` and `n=d^1.5`; `rank(A)=d` independently confirmed at every point -- the span-confinement mechanism is structurally absent). REJECTED anyway: a context-asymmetric skeptic proved `||S||_{I_2}` reduces to a closed form as the Gram matrix `G=AA^T` concentrates toward `I_n` for ANY isotropic random rank-1 family -- at exactly `G=I`, `||S||_{I_2}=max_i\|g_i\|` EXACTLY (order statistic, zero tensor-structure information). Predicted `β=-0.376`/`-0.585` (zero fitted params, from tabulated `E[max_i\|g_i\|]`) vs observed `-0.3673`/`-0.5613` (2.5%/4% gap). Independently re-verified 3 ways: exact recovery on an orthonormal canary (`0.0000%` deficit at `n=80`, matching both the closed form AND an independently-implemented brute-force search); confirmed `run.py` never calls its own claimed `scipy_cross_check` (`grep` returns zero matches); confirmed the "two independent scales" were not independent -- `β_B/β_A=1.528` matches the ARITHMETICALLY FORCED `1.5` ratio (since `n(d)=d^1.5` on Scale B), meaning both scales measured the same `n`-dependence through different `d`-parametrizations, not two separate tests | Да — depends on H-CAT30-1 (`depends_on` edge in `registry/graph.yaml`); no further dependent nodes. Numerical pipeline (power iteration, matricization upper bound, span/rank check) verified correct and reusable; revival requires a NON-isotropic/structured tensor family (e.g. coherent frames) or genuine adversarial search over the family itself, not merely a different `n(d)` scaling |

| H-CAT7-1 | 2026-09-18 | gd-anytime-finite-prefix | REJECT | No prefix-consistent schedule beat the anytime `O(n^{-1.119})` benchmark (ZLDC, COLT 2025) by the pre-registered 5% margin -- train ratios 0.947-0.972 (only `n=4` clears the bar), test ratios `1.88-3.14` (1.9x-3.1x WORSE than benchmark), independently cross-checked via a second implementation (PEPit, max disagreement 1.5e-3, zero verdict flips) and spot-checked again directly against `metrics/verify_main_numbers.json` before accepting. Both HD-MAVP blockers (A1: exact benchmark reconstruction; A2: PEPit cannot express the prefix constraint as a free SDP variable, confirmed via PEPit's own documentation on bilinearity) were resolved cleanly -- no `BLOCKED-INFRASTRUCTURE`. A drafted "substantive positive finding" (ZLDC is a local minimax optimum, mechanistically explained) was sent to a real FL Step 8a context-asymmetric skeptic and found to rest on 5 separate self-confirming artifacts (seeded-start tautology in the dense training run; 12 of 42 local-optimality probes structurally cannot fail; a mislabeled/collapsed 6-schedule "13-point" structural sweep contradicted by its own metrics file; an adversarial prefix-consistency control that cannot report FAIL by construction; an unverified recursive index in the benchmark reconstruction) -- all independently re-confirmed here by direct code/metrics reads, not accepted on the skeptic's word. The skeptic's OWN proposed counter-example (a hand-derived descent direction) was then independently executed and FAILED (`worst_ratio=1.274`, not the predicted `~0.9989`) -- a real error in the skeptic's own report. A follow-up rigorous test (linear program on real envelope-theorem gradients) found a first-order descent direction that ALSO failed on direct execution, in both signs, at 7 step sizes -- a `reviewer` pass then MEASURED the cause directly (one-sided finite differences at `n=3,9,13,17,21` both positive simultaneously, e.g. `n=21`: `D+=+50.97, D-=+7.22`, stable across 3 orders of `eps`): a genuine non-unique-dual-solution kink at exactly the horizons ending on the construction's own long join-steps, confirmed (not merely hypothesized) as the reason single-gradient methods fail in either direction there; 18/23 other horizons match the analytic gradient to 5 decimals, ruling out a sign/indexing bug | Да — brand-new node (registered same day as the REJECT), no dependent nodes at time of this REJECT. The main claim's REJECT is 3-way independently confirmed (original run + PEPit cross-check + this project's own spot-check) and stands untouched by the addenda. The mechanistic "local minimax optimum" finding is WITHDRAWN as genuinely UNRESOLVED (not confirmed, not disproven) -- but its CAUSE (non-smoothness at 5 specific horizons) is now a measured structural fact, replacing the withdrawn Pearl Card with a narrower, better-formed one about the kink itself. UPDATE 2026-09-19: the original builder agent (working unaware of the skeptic/reviewer cascade) independently re-derived the SAME claim-1/claim-3 corrections via different methods, corrected its own "unreachable by ANY schedule" overclaim, and completed the positive control -- revealing its exponent fit understated uncertainty ~3x (cold-start-verified: `-1.1601+/-0.0203`, spread across defensible fits `2.9x` the largest SE). Three independent passes now converge on the same withdrawal via three different methods |

| H-CAT56-1 | 2026-09-18 | pcc-sufficiency-quasipure-eq16-wrong-target | REJECT (verdict as pre-registered; decision.md's own top-level label is `REPEAT`, wrong search space, not merely unfound) | Pre-registered search targeted Eq.(16) quasi-pure states, but the source paper's own End Matter PROVES PCC sufficient for exactly that class unconditionally (LMCC construction) -- a counterexample there is theorem-excluded, not just unfound. 0/400 (pre-registered) + 0/1000 (generic-quasi-pure extension), 1400/1400 explicit saturating POVMs constructed/verified. Structural bound `dim(V_perp)>=r^2+1` proves the certified test (Observation 2) was provably blind at the pre-registered `d=4,r=2` config (5>4) at ANY sample size -- a Gate-1 artifact-identity miss (`artifact-provenance-gates.md`), independently confirmed 3 ways (background agent's own LaTeX read, external user verification, this session's own direct fetch of `arxiv.org/html/2601.21801v1`) | Да — brand-new node, no dependent nodes at time of this REJECT. Kills only the pre-registered claim's specific search-space framing; PROB-CAT56 (the underlying catalog problem) is NOT killed, only correctly narrowed to the genuinely open question (PCC sufficiency for GENERIC quasi-pure states per Ref.[38]/Yang arXiv:2405.00405 Eq.12, not Eq.16's classical-ancilla subfamily). Relaxation Map's V1 (pre-register the generic class) is the natural next step -- no H-CAT56-2 registered yet, pending explicit go-ahead |

> Историческая справка (не эксперимент этой лаборатории, но релевантный NULL upstream): `ART-TAD-AUC-0.99998` инвалидирован 2026-05-16 в проекте H-7 TAD — label circularity. Учтён в `registry/graph.yaml` как артефакт со статусом `invalidated`, ребро `invalidates` от `ART-TAD-R-HONEST`.
