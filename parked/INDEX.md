# parked/INDEX.md — реестр отложенных (валидных, но деприоритизированных)

_Запись здесь означает: утверждение валидно, но отложено. Вернуться, когда сработает revival condition._
_Перед новой гипотезой: `grep -i` по этому файлу — не дублировать отложенную работу._

## Как добавить запись

При вердикте ARCHIVE в `decision.md`:
1. Скопировать заполненный `decision.md` в `parked/<id>-<slug>.md`
2. Добавить строку: почему отложено + **измеримое** условие возврата (не «когда будет время»)
3. Статус узла в `registry/graph.yaml` → `parked`

**Второй допустимый класс записей — `blocked-not-parked`:** гипотеза, у которой в `graph.yaml` статус `blocked` (заблокирована зависимостью, не решением) и **нет** `decision.md` с вердиктом ARCHIVE. Такие строки допускаются здесь только для того, чтобы grep по одному файлу показывал всё отложенное; в колонке `graph node` обязательно явно указан реальный статус. Правило: `blocked` в графе ≠ `parked`; не менять статус графа ради строки в этом файле.

## Index

| id | date | slug | why parked | revival condition (measurable) | graph node |
|---|---|---|---|---|---|
| H-B1-1b | 2026-09-06 | riemann-vs-chromatin-r-stat | upstream artifact `ART-TAD-AUC-0.99998` invalidated; note `[PARKED] Riemann via RMT` is stale (written 12 days after invalidation) | H-7 TAD Option A redesign produces a **new** artifact with honest AUC and pre-registered kill-criterion; `Q-GOE-vs-GUE` resolved or explicitly bracketed | `H-B1-1b` (status: `blocked`, not `parked` — blocked by dependency, not by choice) |
| B4/B5/B6 | 2026-09-06 | frontier-toft-raf-unverified | source projects not found in vault (grep = 0) | user confirms projects exist and gives path, OR confirms they were LLM-generated → then move to `null_results` as `REFUSE(no_source)` | `B4-FRONTIER`, `B5-TOFT`, `B6-RAF` (status: `unverified_source`) |
| H-B3-1o | 2026-09-08 | peterph-lag-diagnostic | half-rise-date-of-raw-series metric failed its own pre-registered positive-control gate (doSat: 77-day gap vs the required ≤20-day tolerance) — underlying question (real delay vs detection-artifact for Peter pH's 100-day-late total-persistence crossing) is NOT falsified, only this cheap diagnostic method is set aside | compare the expanding-tau TRAJECTORY itself (not raw value) between pH and doSat — named explicitly in `decision.md`'s Relaxation Map, machinery (`obrien.expanding_kendall_tau`, `obrien.surrogate_null_curve`) already exists, not yet attempted | `H-B3-1o` (status: `parked`) |
| H-CAT31-3-POL | 2026-09-18 (extended same day, Points 93→95, epistemic-status corrected Point 99) | active-search-through-point95 | `(POL): sup_n E‖x*‖²<∞` proof-attempt line (Points 77-92): 4 independent proof routes killed (Talagrand transfer, uncertainty-principle-on-supports, Young's/sup-norm, KKT/argmin-stability); `mean(CV²)` model-comparison formally closed as uninformative over `n∈[127,8009]`; Bandeira-proof-structure audit shows `(POL)` would close only the `log log n` ORDER gap, not the sharp constant; both of Point 90's named "cheapest next steps" (91-92) killed/corrected. **Extended 2026-09-18** to cover the two follow-ups attempted after PARK: `F4rel` (gated on the already-parked `(POL)`, no independent route — Point 92) and `R_n` (Point 95's pre-registered tightened-reps follow-up — same-model context-asymmetric review, `INTERNALLY CROSS-CHECKED` per Point 99, found the weighted 95% CI on `R_n`'s scaling exponent contains both `b=0` and `b=1`, i.e. does not discriminate). No further ready, non-tautological cheap next step for this experiment as of Point 95 — pause, not kill | a genuinely new `(POL)` proof technique not among the 4 killed routes; OR new literature bounding `CV²`-type LP/SDP-optimizer quantities (Gate 0 found none as of 2026-09-16); OR a bug-free low-density structured follow-up to Point 91; **OR for `R_n` specifically — EXECUTED 2026-09-18 (decision.md Point 98): C1/C2/C3 revival INCONCLUSIVE → re-PARKED**: combined old+new draws, equalized to 800-1000 reps at n=509/1021/2039, BCa+nested-bootstrap — corrected exponent CI `[-0.04,1.01]`, same bucket as Point 95 even at 5x the sample size. Updated revival condition: one additional point at `n≈8191` (pre-registered ~3.5σ-differentiating kill criterion named in `parked/H-CAT31-3-pol-proof-attempt.md`), not yet attempted | `H-CAT31-3` (status: broader node already `REJECTED` for an earlier, narrower exponent claim; this entry parks the derived `(POL)`/`F4rel`/`R_n` active-search effort, not the project's own original `Var(log(θ(G)/√n))=O(1/n)` target, which remains open — **and gained supporting empirical evidence on 2026-09-18** (not proof of the asymptotic rate — Point 99): the R_n revival's pooled data, fit directly on `n·Var(X_n)`, gives `b_Var=-0.042±0.046`, 95% CI `[-0.13,0.05]`, consistent with `O(1/n)` at these 3 `n`) |
